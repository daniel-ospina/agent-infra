<!-- research-path: docs/research/2026-08-29-issue-383-research.md -->

# Vendor parallel_work_check + No-Board Checkpoint Pass — Implementation Plan (#383)

> **For Pi:** Use `executing-plans` to implement this plan task-by-task.

**Goal:** Make the parallel-check checkpoint gates (C1–C5) passable for no-board pipelines and vendor the checker into `agent-infra/scripts/` — one PR satisfying issue #383's O/I/T (fresh clone has the checker; a non-swarm session passes plan+implement checkpoints without the human-only force-pass; swarm behavior unchanged; enforcement SKILL.md path matches reality).

**Team:** organisation-design-team
**Role:** (unavailable)

**Architecture:** Card A — predicate-in-checker ("writer-side skip, gate consumes"). The vendored Python checker owns tenant detection (`_is_no_board`: skip iff ALL of SWARM_CARD_ID/CARD_ID, board URL+key, SWARM_TOUCHED_PATHS/TOUCHED_PATHS, AGENT_ID/SWARM_AGENT_ID are absent; ANY signal → fail-closed byte-for-byte). No-board runs skip pure-board sub-checks with a distinguishable `CLEAR no-board-skip: <advisory>` verdict + `mode:"no-board-skip"` token field (never a byte-identical vacuous CLEAR); git-local checks (C1 fetch/guard/dup-search, C4 fetch/behind/symbol-gated pickaxe) are retained. The enforcer gains repo binding on the real-token path via a mode-aware wrapper (gate/strict only) + a distinct `checkpoint_no_board_skip` audit. Distribution = vendor from swarm canonical @ `29dd67e8` + patch ledger + drift job. Full sub-decisions S1–S12, evidence, and review-cycle logs: `docs/scoping/2026-08-29-issue-383-plan.md`.

### Pattern Research

> **Findings date:** 2026-08-29

> **Gate skipped: plan touches zero third-party dependencies** — the checker (Python) is stdlib-only (urllib REST); connectors/ are stdlib at module level (supabase-py lazy inside `_client()`, untouched); the enforcer (TS) is zero-dep; tests use pytest/pytest-timeout (CI-installed, not authored against). No library version/API/idiom/pitfall buckets apply. Prior research intake (Step A) consumed the scoping artifact: `### Axis Research` + `### Integration Docs` in the issue comment + `docs/research/2026-08-29-issue-383-research.md` (skip-semantics pitfalls, vendoring precedents, python-ci install obstacle — all already triangulated in the Phase 1.5 artifact; the scoping plan's §11 review cycles 1–6 ran to all-NO-ISSUES-FOUND).

### Integration Surface Map

| Surface | Boundary | Test Layer | Test(s) | Bug Pattern Flags |
|---|---|---|---|---|
| Checker env-seam (run_check → per-check dispatch) | env signal families → predicate | unit (env-seam, injectable board/store/git/gh) | B1–B34 | silent-skip on missing config; vacuous-CLEAR; pickaxe(None) TypeError |
| Token contract (payload write/delete) | `_apply_token` → token file | unit | B14/B15/B28/B30, T5/T10 | missing mode field; byte-identical skip vs CLEAR; partial-write race; silent write-failure |
| Repo binding (checker remote_url ↔ enforcer bindingRepo) | `git remote get-url origin` parity | unit (TS) + unit (py, FakeGit/RealGit) | T1–T16, B18/B26/B32 | `config --get` vs `remote get-url` insteadOf divergence; abspath-vs-URL mismatch; both-"unknown"; path-valued env |
| Checkpoint gate (escape regex, TTL, guidance, advance) | checker invocation ↔ enforcer token read | unit (TS escape matrix) + unit (advance sites) | T1–T16 + escape-matrix positives (Task 3 Step 1) | `$VAR` execution rejected; silent-stall; fresh-token guard on binding-fail; audit emission count |
| Audit sink (enforcement.jsonl) | event emission | unit (auditRelease) | T6 | new event schema; mode-domain ambiguity; audit hygiene (wc -l unchanged) |
| Gated skills + enforcement SKILL.md | instruction text ↔ escape allowlist | docs grep (Phase D) | `grep` zero hardcoded swarm paths; `$AGENT_INFRA_PATH/scripts` ≥ 1 | stale fiction; CHECKOUT_GUARD_ENFORCE drift |
| Vendored distribution | swarm HEAD ↔ agent-infra copy | CI (vendor-drift --manifest) + bash -n | B20 + ci.yml verify job | silent drift (supabase_swarm class); cross-layout patch headers |
| CI (python-ci.yml reusable, ci.yml, ci-main.yml) | caller ↔ reusable workflow | CI | verify job + python-ci caller + workflow-drift | base-ref resolution (guard + caller same PR); unconditional `pip install -e` |
| Realistic no-board session | actual consumer env | integration probe (B17/B21 + Phase F 27) | B17/B21 | generic-Supabase env collision; scrubbed capture |

### Verification Plan

Domain: **config/operations** (agent-infra infra; no UI, no ontology, no content). Complexity: Org Infra standard, Config standard, Library-deps low.
- **Unit (py)**: full vendored suite (34 tests: 32 pass / 1 xfail-or-xpass / 1 skip — incl. the deterministic timeout-contract sibling test; the xfail/strict=False covers the swarm-parity mock race — on the vendored layout it PASSES deterministically (xpassed), so the timeout contract stays asserted by the sibling; skip counts the supabase-v2 skipif — absent-supabase-py machines run 32/1/1, supabase-py-present machines run 33/1/0) + B1–B34 env-seam tests — per-PR via the ci.yml `verify` job.
- **Unit (TS)**: enforcer suite 129 existing + T1–T16 (+ escape-matrix positives + legacy-fixture policy) — per-PR via the ci.yml `verify` job (zero-dep tsx).
- **Integration/probes**: B17 (fresh consumer checkout, minimal env) + B21 (consumer-env fixtures) + Phase F step 27 manual probe (hard gate, split pre/post-merge + lag tripwire).
- **Config validation**: `scripts/check-vendor-drift.sh --manifest` + `bash -n` on vendored .sh + workflow-drift (templates == materialized) — ci.yml `verify`/`vendor-drift` jobs + pipeline-compliance.
- **Skipped**: UX verification (no UI — instructional surface reviewed in scoping Phase 7 Agent #2), content/research domains (n/a).

**Tech Stack:** Python 3.10+ stdlib (checker), Node 22 + tsx (enforcer tests), bash (wrapper/guard), pytest + pytest-timeout (CI-installed), GitHub Actions (reusable workflows).

---

### Task 1: Vendor the checker suite + provenance (Phase A)

**Intent:** A fresh agent-infra clone must have the full checker runtime + tests (the issue's indicator 1), and the vendored copies must be drift-guardable. Without this task the Python suite cannot even collect (fake_supabase.py missing) and C1's guard dependency is absent.

**Acceptance:** `git ls-files` lists `scripts/parallel_work_check.{sh,py}`, `scripts/checkout_guard.sh`, `scripts/fake_supabase.py`, `scripts/test_parallel_work_check.py`, `connectors/{__init__,supabase_swarm,supabase_org,hosted_tortoise}.py`, `scripts/testdata/consumer-env-*.env` (redacted), `VENDOR.md`, `scripts/.vendor-manifest.json`, `scripts/check-vendor-drift.sh`; `python3.12 -m pytest scripts/test_parallel_work_check.py -x --timeout=60 -q` (the pinned interpreter per Step 4) → exit 0 + ≥ 32 pass + 1 skip, with the timeout test xfail-or-xpass (strict=False — see Step 4); `scripts/check-vendor-drift.sh --manifest` exit 0; `bash -n` clean on the vendored .sh files.

**Files:**
- Copy (from `~/swarm` @ `29dd67e8`): `scripts/checkout_guard.sh` (operations/coordination/), `scripts/fake_supabase.py` (tests/), re-copy `connectors/supabase_swarm.py` + `connectors/__init__.py` (connectors/ — replaces the 28-line-stale working-tree copy; re-diff → identical; `__init__.py` is part of the vendored package and MUST be drift-guarded too)
- Create: `VENDOR.md`, `scripts/.vendor-manifest.json`, `scripts/.vendor-patches/`, `scripts/check-vendor-drift.sh`
- Modify: `.gitignore` (+ `scripts/heartbeats/`)

**Step 1:** Copy the 3 files from swarm HEAD (`git -C ~/swarm show HEAD:<path> > <dest>` or worktree copy); re-diff `connectors/supabase_swarm.py` vs swarm → must be identical; VERIFY (re-copy + re-diff if needed) `connectors/supabase_org.py` + `connectors/hosted_tortoise.py` from swarm @ `29dd67e8` — they are already identical in the tree, but provenance + drift-guarding must be unambiguous for ALL FOUR connector files; record the pristine-@-base file set. **IMPORTANT — `scripts/parallel_work_check.py` is NOT re-copied:** the working tree carries a load-bearing 21-line / 3-hunk vendored divergence from swarm @ `29dd67e8`: (1) the ancestor-probe for `connectors/` that fixes `_REPO_ROOT` for the agent-infra `scripts/` layout (10 lines — swarm's `parent.parent.parent` resolves to the wrong dir under this layout and `from connectors.supabase_swarm import store` would fail → the checker degrades to UNKNOWN); (2) the two pre-existing partial CLEAR branches — `no-card-no-scope` in `_check_c3` (~576-581) and `no-card` in `_check_c4` (~624-628) — the '#383 partial' the issue comment describes and Task 2's B24 deletes. The FULL 3-hunk diff is ledgered as vendored delta P# (see Step 5); a re-copy MUST be followed by patch re-application, and the manifest records the patched sha.
**Step 2:** Restore the 2 `CHECKOUT_GUARD_SWARM_ROOT` fixture lines in `scripts/test_parallel_work_check.py` at the positions swarm HEAD has them (after the `_bash_env` calls in `test_bash_c1_clear_end_to_end` + `test_bash_c1_stale_end_to_end`).
**Step 3:** Add `@pytest.mark.xfail(strict=False, reason="known swarm-main failure — timeout-mock race; upstream fix tracked (swarm issue filed); vendored parity 32/1/1 — on the vendored layout this test PASSES deterministically (xpass under strict=False); the sibling owns the timeout contract")` to `test_bash_timeout_unknown_and_exit_zero` — xfail covers the swarm-parity flake ONLY. Add the deterministic sibling `test_bash_timeout_contract`: run the real `.sh` wrapper with phase `start` against the local HTTP mock with `PARALLEL_CHECK_TIMEOUT_SECS=0.05` and a guaranteed-slow mock route (sleep on the mock, not real-time racing) → assert `C1: UNKNOWN` first line (the wrapper derives the code from the phase — `C1` for start; `C?` appears only on the missing-phase form) + exit 0 + NO token written. **Write-then-hang watchdog window (the wrapper's hard `kill -9` at budget+2s has a second untested path):** a mock `_apply_token` that writes a CLEAR token then blocks → the watchdog kills python before the verdict flushes → assert the wrapper emits UNKNOWN AND the token file is ABSENT afterward + NO `*.tmp*` residue remains. **This REQUIRES a small patch to the vendored `parallel_work_check.sh`: the current wrapper (byte-identical to swarm @ `29dd67e8`) does NOT unlink the token on `rc != 0` — it only echoes the verdict line — and a watchdog SIGKILL skips python's own cleanup, so a stale CLEAR token would survive and the enforcer's marker advance (~1551, which reads only the token) would advance on a check the operator just saw fail. Add to the wrapper's error branch: unlink the token file on `rc != 0` AND glob-clean ONLY the token file's OWN tmp prefix — `rm -f "$(dirname "$token_file")/$(basename "$token_file")*.tmp.*"` (SIGKILL bypasses python's `finally` — the wrapper glob is the only cleanup on that path; an UN-scoped `*.tmp.*` in `/tmp` would delete unrelated same-user tmp files AND a concurrent invocation's in-flight unique tmp — destructive; scope it to the prefix), AND clamp the watchdog's budget computation to the SAME max the python side clamps to — **`PARALLEL_CHECK_BUDGET_MAX`, env-read with DEFAULT 60 seconds, read by BOTH the `.sh` watchdog and the python budget clamp — TEST-INJECTABLE: the wrapper-level fold tests set a small max (e.g. 0.2s) so the watchdog fires fast for the non-finite/upper-bound cases (<1s wall clock), and a fast unit assertion pins the default == 60; do NOT combine a hardcoded 60 clamp with the `--timeout=60` suite timeout — the clamped watchdog fires at ~62s, which `--timeout=60` would kill before the assertion (any residual long-path test gets `@pytest.mark.timeout(90)`)** (the `.sh` computes `budget + 2.0` from the RAW env var — an unbounded value would stall the watchdog indefinitely; the B23 upper-bound fold requires it to fire within bounded time). Register both as the ledger patch (`scripts/.vendor-patches/parallel_work_check.sh.patch`, Step 5) so drift expectations stay exact. ONE tmp naming convention everywhere: `<tokenfile>.tmp.<pid>` — Task 2 Step 4's write, B30's assertion, this glob, and the hang test's residue assertion all derive from it.** **Pre-rename hang variant:** a mock that blocks inside serialize BEFORE the rename (mid-tmp-write) → the watchdog kills it → assert the same invariants (UNKNOWN verdict, no token, NO `*.tmp*` residue via the wrapper glob). **Watchdog-kill residue pins: (a) the heartbeat/guard-log file REMAINS after the kill (documented accumulation — there is NO per-run heartbeat cleanup path and none is added; pinned so the VENDOR.md cleanup-procedure note cannot silently rot); (b) the full process tree is REAPED — start the python child in its own process group (`setsid`) and have the watchdog `kill -9 -<pgid>` on fire, so a hung `git fetch` subprocess dies with python instead of orphaning (tested with a `sleep`-backed fake `git` shim on PATH as the fetch target, asserting no surviving process after the watchdog fires).** This owns the bash-level timeout contract the xfail cannot assert (strict=False is satisfied by both pass and fail; a regression that hangs or writes a token on timeout would otherwise be CI-green indefinitely).
**Step 4:** Pin the interpreter + deps FIRST: `python3.12 -m pip install pytest pytest-timeout` (or a venv; the system `/usr/bin/python3` is 3.9.6 WITHOUT pytest-timeout, and the suite targets 3.10+ — `python3.12` exists at `~/.local/bin/python3.12`). Then run: `python3.12 -m pytest scripts/test_parallel_work_check.py -x --timeout=60 -q` → ≥ 32 passed / 1 xfail-OR-xpass / 1 skipif-supabase-v2 skipped (32/1/1 on this machine — the middle is xfail-or-xpass: the vendored layout runs the timeout test PASSING deterministically, so pytest reports 1 xpassed under strict=False; the sibling `test_bash_timeout_contract` is the load-bearing timeout assertion either way; 33/1/0 where supabase-py is installed — the count qualifies; the timeout-contract sibling must be present).
**Step 5:** Write `VENDOR.md` (base rev `29dd67e8`, per-file table INCLUDING `connectors/__init__.py` — identical@base, drift-guarded; patch ledger P1–Pn with machine-readable forward patches at `scripts/.vendor-patches/<file>.patch` — headers with agent-infra-relative paths so `git apply -R` works from the repo root; ledger includes the test-file patches + xfail + **the `parallel_work_check.sh` patch (Step 3: unlink-on-`rc != 0` + scoped tmp-glob + watchdog budget clamp) + the `parallel_work_check.py` patch (the full 3-hunk pre-existing divergence: `_REPO_ROOT` ancestor-probe + the two partial CLEAR branches, ledgered as P#)**; sync + conflict-handling procedure; heartbeat note (the contract is the GUARD-LOG file-count contract): `scripts/heartbeats/` is gitignored and holds the guard's single accumulating `checkout_guard.log` (the checker itself writes NO heartbeat files — `heartbeat_at` is a supabase lease column; per-run heartbeat files with clean-exit cleanup would be a NEW vendored divergence, deliberately NOT introduced); growth is bounded (≤ 1 file, accumulating), watchdog-killed sessions leave the log (no per-run cleanup path — pinned by the Task 1 Step 3 residue assertion), cleanup is the documented manual procedure; **guard-config note (matches B33's two cases): the vendored `checkout_guard.sh`'s default swarm-root is the agent-infra checkout (`$CHECKOUT_GUARD_DIR/../..`) — under the canonical invocation that is the MAIN checkout, NOT a worktree: `_guard_is_swarm_root(worktree)` is false, so the main/worktree collision checks (which swarm's own invocation runs) are SKIPPED for worktree sessions (foreign-checkout policy → dirty-check-only verdicts). BOARD-mode worktree sessions MUST set `CHECKOUT_GUARD_SWARM_ROOT` to their swarm checkout to keep parity — the unset default diverges and that divergence is the documented deployment delta. ALSO note the inherited `origin/main` assumption: fetch/behind target `origin/main`; consumers with a non-main default branch fail closed UNKNOWN (pinned by B17 non-main variant), and the C1-guard-behind (foreign → 0) vs C4-behind (blocks) asymmetry is inherited from swarm — documented, not re-balanced. ALSO note the `origin`-required assumption: a consumer with no `origin` remote (fork layout, only `upstream`) fails closed UNKNOWN on C1/C4 with binding passing via both-"unknown" (pinned by B17 renamed-remote variant) — guidance names the `origin` requirement**) + `scripts/.vendor-manifest.json` (identical@base sha256 + patched upstream_sha256, covering ALL vendored files incl. `__init__.py`) + `scripts/check-vendor-drift.sh` (`$SWARM_ROOT` live mode + `--manifest` reverse-apply mode) + `.gitignore` += `scripts/heartbeats/`. Add the AGENTS.md REPO-SPECIFIC routing line ("Vendored swarm artifacts: see `VENDOR.md`").
**Step 6 (the load-bearing pre-merge signature capture):** Capture live `env` from GENUINE no-board consumer sessions — a fresh `pi -p` process launched INSIDE a consumer checkout (tortoise, DMeer — ≥ 2 distinct repos), `env` captured with VALUES REDACTED (variable NAMES preserved verbatim). Assert: (a) real-session markers present (PI_*/GH_TOKEN/PATH); (b) capture-context repo affinity — `PWD` + `git remote get-url origin` of the capturing context ∈ the consumer-repo remote set (tortoise/DMeer), mirroring the real-session assertion. Commit `scripts/testdata/consumer-env-*.env` (redacted VALUES) **+ `scripts/testdata/consumer-env-*.names` (the RAW NAME INVENTORY — every name verbatim, values stripped — the evidence the pre-merge human gate reviews for the S1 collision fork)**; **the no-leak gate is redact-then-diff, not grep-the-marker: for each secret-bearing name (GH_TOKEN and any other value-bearing name), assert the committed value DIFFERS from the captured value, plus a strict allowlist check — no non-marker values outside an explicit allowlist of names.** **The implementer session's own env is NOT an acceptable capture** — it has PI_*/GH_TOKEN/PATH and no board signals, so it would vacuously pass B21 without ever sampling a consumer environment. These fixtures feed B21 (Task 2) and the pre-merge probe (Task 6).
**Step 7:** File the swarm upstream issue for the red baseline + the follow-up issue for the re-probe cadence (S1 pin). Run: `scripts/check-vendor-drift.sh --manifest` → exit 0; `bash -n scripts/checkout_guard.sh scripts/parallel_work_check.sh scripts/check-vendor-drift.sh` → clean. Commit (staged per-file, incl. `scripts/testdata/`).

### Task 2: Checker predicate + distinguishable skip (Phase B, TDD)

**Intent:** The core behavior fix — no-board tenants get an honest, audit-distinguishable pass; board sessions stay byte-identical; the partial's vacuous CLEARs are eliminated; the pickaxe hoist is symbol-gated so no-board mode cannot introduce a pickaxe(None) TypeError (a prospective hazard of the new branch, not a current bug — both existing call sites are already symbol-gated).

**Acceptance:** `_is_no_board(env)` five-independent-absent-conditions with **pinned value semantics: absent = unset OR empty/whitespace (stripped value-truthiness)** — ANY non-empty signal → fail-closed; **the signal set is the EXACT env-name set `_sb_config` reads — 8 names: `PARALLEL_CHECK_SB_URL`, `SUPABASE_URL_ORG_DATA`, `SUPABASE_URL`, `PARALLEL_CHECK_SB_KEY`, `SUPABASE_SERVICE_ROLE_KEY_ORG_DATA`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY_ORG_DATA`, `SUPABASE_ANON_KEY` — shared via ONE constant that BOTH `_sb_config` and `_is_no_board` read, with NO prose re-enumeration, and the SAME mandate extends to ALL FIVE families: the card/agent/paths name sets are derived from the constants `run_check` reads** (so "fail-closed on ANY signal" is true by construction and the predicate can never diverge from the read paths — a names-parity B-test AND the 14-name B23 value-semantics parameterization enforce it); C1–C5 emit `CLEAR no-board-skip: <advisory>` + `mode:"no-board-skip"` token field; board-mode-no-card → swarm-parity UNKNOWN via retained guards; C4 no-board branch fetch→behind→symbol-gated pickaxe→skip (retained-check failures → UNKNOWN, never skip); token payload `mode` + URL-form `repo`, written **atomically (tmp in `dirname(token_file)` + `os.rename`)**; `main()`'s exception-path token write signature-synced under the new contract (no `repo:""`/no-mode CLEAR-shaped tokens); B1–B34 green (incl. empty/whitespace semantics over the full 14-name signal set, concurrency + atomic mechanism + watchdog-cleanup×concurrent-writer, write-failure + stale-token fail-closed, retained-check failure edges, board-mode guard parity two-case); full suite 32+B green; `grep -c "no-board-skip" scripts/parallel_work_check.py` ≥ 1; `--manifest` regenerated + exit 0.

**Files:**
- Modify: `scripts/parallel_work_check.py` (predicate over the shared `_sb_config` signal constant, `_Ctx.is_no_board`, `CheckResult.mode`, `_skip`, C1/C2/C3/C4/C5 branches, pickaxe hoist + symbol gate, `GitOps.remote_url()`, cwd-based repo resolution, budget parse clamp (`PARALLEL_CHECK_BUDGET_MAX`), `_apply_token` mode/repo + atomic write, `main()` exception-path token write, module docstring INTERFACE)
- Modify: `scripts/test_parallel_work_check.py` (B1–B34)

**Step 1 (tests first):** Add `_env_noboard()` + `FakeGit.remote_url()`; write B1–B34 (per the scoping plan §5 table: no-board skips per check, negative fences B4/B5/B7/B9/B12/B19/B23, guard/dup precedence B2/B3, pickaxe hoist + symbol-gate B11/B25, token mode/repo B14, distinguishable contract B15, bash e2e B16 (with `CHECKOUT_GUARD_SWARM_ROOT=str(repo)` + slug), realistic probe B17, resolution B18 (insteadOf-scoped, slug outcome pinned), drift tests B20, consumer-env fixture B21, board-mode resolution B22, single-family fence B23, vacuous-CLEAR deletion B24, override parity B26, stale-clone + warn emission B27). **B23 extended — the load-bearing predicate boundary (P1):** parameterized over ALL FIVE families' shared constant names — `_sb_config`'s 8 board names (`PARALLEL_CHECK_SB_URL`, `SUPABASE_URL_ORG_DATA`, `SUPABASE_URL`, `PARALLEL_CHECK_SB_KEY`, `SUPABASE_SERVICE_ROLE_KEY_ORG_DATA`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY_ORG_DATA`, `SUPABASE_ANON_KEY`) + the 6 card/agent/paths names (`SWARM_CARD_ID`/`CARD_ID`, `AGENT_ID`/`SWARM_AGENT_ID`, `SWARM_TOUCHED_PATHS`/`TOUCHED_PATHS`) — 14 names, derived from the constants, NEVER prose (the acceptance's earlier 16-name enumeration was wrong and is removed) — the value-semantics boundary is TESTED across all five families, not just constructed (a presence-keyed bug in the card/paths families — e.g. `SWARM_CARD_ID=""` exported in a no-board shell tripping the predicate — would otherwise pass every test). **Five-family parity (P2 pin): the shared-constant mandate extends to the OTHER families — the card (`SWARM_CARD_ID`/`CARD_ID`), agent (`AGENT_ID`/`SWARM_AGENT_ID`), and paths (`SWARM_TOUCHED_PATHS`/`TOUCHED_PATHS`) name sets are derived from the SAME constants `run_check` reads, and a names-parity B-test fails if any env name appears on one side's constant but not the other (an alias added to one read path but not the predicate would silently reclassify a board session as no-board). ALSO fold the remaining injectable env names (`GH_API_BASE`, `GH_REPOSITORY`, `PARALLEL_CHECK_REPO_SLUG`) into the empty/whitespace parameterization — each must yield its documented fallback (e.g. whitespace `GH_API_BASE` must not produce a malformed `   /search/issues` URL → InfraError → UNKNOWN). **Edge-value fold (P1 pin — `PARALLEL_CHECK_TIMEOUT_SECS` + `PARALLEL_CHECK_SYMBOL`):** parameterize `PARALLEL_CHECK_TIMEOUT_SECS` over `""`/`"   "`/`"0"`/`"-1"`/`"abc"` → the verdict line must NEVER be blank (clamp to the default/UNKNOWN — a `float("")` crash plus the `.sh` wrapper's `head -n 1` yields a BLANK verdict line, breaking the verdict contract), the watchdog must still fire (or a documented min-budget clamp applies), exit 0, no token written; and `PARALLEL_CHECK_SYMBOL` over `""`/`"   "` → the symbol gate does NOT fire (absent → matches the no-symbol verdict; a whitespace symbol presence-keyed as truthy would fire a dup-search with a blank query in a no-board session — a surprise block one stray var from the covered set). **And the UPPER-bound + NON-FINITE edges: `PARALLEL_CHECK_TIMEOUT_SECS=999999999` AND the float-parsable non-finite set `"inf"`/`"nan"`/`"1e309"` (these parse WITHOUT ValueError, so the `except ValueError → default` does NOT catch them; `budget=inf` → `sleep inf` on GNU coreutils sleeps forever → the watchdog never fires — the indefinite-stall class this fold exists to close) → both python and `.sh` sides CLAMP to `PARALLEL_CHECK_BUDGET_MAX = 60` (the value pinned in Task 1 Step 3), the watchdog fires within max + margin (asserted wrapper-level with the real `.sh` + slow mock), the verdict line is not blank, exit 0, no token written.** × `""` and `"   "` → assert BOTH the predicate AND the resulting verdict (skip vs fail-closed). Pin: **empty/whitespace = absent** (value-truthiness on stripped values). Rationale: (a) no-board consumer shells ubiquitously export empty vars (e.g. `SUPABASE_URL=""`) — presence-keyed semantics would break indicator 2 in exactly the environments the probe worries about; (b) the fail-closed guards protect the PARTIAL cases, NOT the all-empty case: an all-empty session IS indistinguishable from a no-board session under the pinned semantics and CORRECTLY skips (a documented, accepted Option-A property); any single non-empty board signal → board mode → the retained guards fire (e.g. `SUPABASE_URL` set + card_id empty → C3 `missing-card-context` UNKNOWN). **B17 variant:** no-board env + `PARALLEL_CHECK_SYMBOL` set + no slug + a local-path (non-github) origin → assert `C1: UNKNOWN` (repo-slug-undeterminable), NOT skip-CLEAR — pins the documented S1 residual that a symboled no-board session cannot pass C1. **B17 non-main variant (inherited `origin/main` assumption, documented not fixed):** a no-board consumer whose origin has no `main` ref (default branch `master`) → `git fetch origin main` fails → C1/C4 fail-closed UNKNOWN (never skip) — pins the current behavior as a documented limitation (VENDOR.md notes it; swarm upstream owns the fix — the C1 guard-delegated behind-check returns 0 for foreign checkouts while C4's own behind-check blocks: the asymmetry is inherited, documented in VENDOR.md, NOT silently re-balanced). **B17 renamed-remote variant (fork layout):** a consumer with NO `origin` remote (only `upstream`) → `GitOps.remote_url()` fails → `"unknown"` on BOTH checker and enforcer sides (binding passes via both-"unknown") while C1's fetch targets `origin/main` → fails → fail-closed UNKNOWN (never skip-CLEAR) — an unpassable-gate class for forks, documented in VENDOR.md alongside the non-main note (remediation: guidance names the `origin` requirement). **B21 assertion pinned (NO vacuous pass — the load-bearing capture evidence):** assert the fixture's board-signal NAMES are each either ABSENT or EMPTY-valued (empty = absent under the pinned B23 semantics) AND the real-session markers + repo affinity hold; the pre-merge human gate reviews the RAW-CAPTURE NAME INVENTORY (values stripped — see Task 1 Step 6) as the evidence the S1 collision fork needs — reconciled with redact-then-diff by keeping names verbatim. **Minimum fixture-set gate: assert ≥ 2 fixtures, each with a DISTINCT captured origin in its affinity evidence — a partial capture (one consumer repo) fails the pre-merge gate rather than degrading the collision evidence.** **B28 (concurrency, DETERMINISTIC — the ordering is the test's subject, not the machine's race):** two checker invocations on the SAME token file with controlled interleaving — a threading barrier makes the STALE run's unlink provably complete AFTER the CLEAR write → assert the gate outcome is a BLOCK with the specific "none found"/stale named reason; a second case pins the reverse ordering (CLEAR last) → gate PASSES (last-writer-wins semantics). **B29 (write-failure, operator-visible surface pinned):** unwritable token path (chmod 000 dir — DROP the nonexistent-dir sub-case: `_apply_token` calls `os.makedirs(exist_ok=True)`, so only chmod-000 exercises the failure) → the VERDICT LINE carries a failure note: `C1: CLEAR no-board-skip warn=token-write-failed` — the sole stdout line is preserved (the `.sh` wrapper discards stderr and emits `head -n 1` of stdout, so an unmarked stdout warn would be emitted AS the verdict — contract corruption; and a stderr-only warn is invisible through the canonical wrapper, so the verdict note is the operator-visible signal; the enforcer's "none found" BLOCK remains the backstop). The `.sh` may additionally forward stderr for direct-python invocations. Wrapper-level assertion: the verdict line stays the sole first stdout line, with the warn note appended, in the failure case. **B29 stale-token extension (P1 pin — the backstop must actually hold):** monkeypatch a write failure that leaves the dir READABLE (ENOSPC — chmod-000 is the T10(d) unreadable-token class, a different failure) with a PRE-SEEDED stale same-phase CLEAR token on disk (the token file is machine-global and never unlinked on a token-driven advance — T12): pin the fail-closed outcome — `_apply_token`'s failure path UNLINKS any existing token so the enforcer's "none found" BLOCK holds (asserted via the joint checker→enforcer harness), the final token path is ABSENT after the failed write, and the verdict note `warn=token-write-failed` is present. Without this, a later ENOSPC run advances the gate on the PREVIOUS session's pass over an operator-edited tree. **B29 unlink-failure corner (P1 pin — the stale-token fail-open must not be SILENT):** monkeypatch `os.unlink` to raise (EACCES/EROFS — read-only token dir) on BOTH (a) the UNKNOWN-branch unlink and (b) the write-failure cleanup unlink, with a pre-seeded stale same-phase CLEAR token → assert the failure surfaces as a verdict-line `warn=unlink-failed` note (the 'never silent' discipline applied symmetrically); CLOSE the corner by extending the `.sh` patch: the wrapper unlinks the token on `rc != 0` OR a non-CLEAR first-line verdict (UNKNOWN verdicts exit 0 by design — the wrapper must parse the verdict, not trust rc); the read-only-dir-both-unlinks-fail corner is documented in VENDOR.md as an accepted environmental fail-open WITH the operator-visible note (never silent).** **B30 (atomic MECHANISM — the invariant itself, not just its neighbors):** monkeypatch `os.rename` to raise mid-write → the final token path is unchanged/absent (never a partial token) **AND the tmp file is ABSENT after the raise (the cleanup is a `finally` in `_apply_token` — Step 4 — so an exception path leaks no residue)**; assert the temp file resolves to `dirname(token_file)` (same-directory rename — `os.rename` is atomic only within a filesystem) **AND that the tmp NAME embeds a per-invocation unique component (pid/random) — a fixed name (e.g. `token.json.tmp`) lets two concurrent writers interleave on ONE tmp path (writer A's rename fires mid-B's serialize → corrupt payload) while B28's barriers order only the token-file ops; a unique tmp name makes the atomicity claim unconditional.** **B31 (retained-check failure edges in no-board mode — split by the phase each check actually runs in):** (a) FakeGit.fetch raising InfraError at C4 (fetch runs in `_check_c4`) → `C4: UNKNOWN` (NEVER skip-CLEAR) + no token + exit 0; (b) guard timeout + dup-search 500 at C1 (both run in `_check_c1`) → `C1: UNKNOWN` — pins the fail-closed contract of every retained git-local check in no-board mode (an offline no-board session must not silently pass). **B32 (repo env chain empty/whitespace):** `PARALLEL_CHECK_REPO=""`/`"   "` falls through to cwd resolution — the token carries the resolved remote, never `""` (presence-keyed resolution would BLOCK every genuine no-board session one env name away from the B23-covered set). **B33 (board-mode guard parity — TWO pinned cases, no false premise):** (a) SWARM_ROOT-SET parity path: `CHECKOUT_GUARD_SWARM_ROOT` pointed at a board worktree checkout → main/worktree-collision verdicts match swarm's own invocation (C1 guard DEFER on a non-main worktree branch, etc.); (b) UNSET default: the vendored guard's default root resolves to the MAIN checkout via `$CHECKOUT_GUARD_DIR/../..` under the canonical invocation → the worktree is FOREIGN (`_guard_is_swarm_root(worktree)` false) → foreign dirty-check-only verdicts (main/worktree collision checks SKIPPED) — assert THIS as the actual vendored default and DOCUMENT the divergence (parity requires the env override; the VENDOR.md note says exactly that). **B34 (watchdog cleanup × concurrent writer):** two wrapper invocations on ONE token dir — session A hung long enough for the watchdog to fire (with a mid-write `<tokenfile>.tmp.<pid>` on disk), session B completing a CLEAR write → assert the final token is EITHER the surviving CLEAR (intact) OR absent (fail-closed BLOCK), NEVER a partial/corrupt token; pin the spurious-BLOCK outcome as the accepted worst case (A's scoped glob deletes B's unique tmp → B's `os.rename` raises → B's write-failure path unlinks the token → fail-closed; B28's barriers order only token-file ops, so this interleaving is the untested complement). **Deterministic scheduling variant (the glob-vs-rename interleaving FORCED, not raced — the union assertion alone lets CI run forever on the easy ordering):** a mock `os.rename` that blocks until a barrier, lets the concurrent invocation's wrapper glob run first, then resumes with a raise → assert the rename-raise → unlink → absent-token fail-closed outcome with the `warn=token-write-failed` note. **Plus the heartbeat contract (folded here, pinned to the ACTUAL mechanism): `scripts/heartbeats/` holds ≤ 1 file (the guard's single accumulating log — the checker writes NO heartbeat files; per-run heartbeat files with clean-exit cleanup would be a NEW vendored divergence, deliberately NOT introduced), the count stays bounded across N consecutive runs, and a watchdog-killed session leaves the log (documented accumulation — the VENDOR.md note is prose today; this pins the contract).** Run → red.
**Step 2:** `CheckResult.mode` slot + `_skip(ctx, detail)` helper; `_is_no_board(env)` + `_Ctx.is_no_board` set in `run_check` (after env resolution, before board construction). **B27 mechanism (near-miss + new-signal warns — specified here, from scoping §5 B27):** after the predicate, `run_check` emits (a) a near-miss warn when EXACTLY ONE board-signal-family member is non-empty → UNKNOWN verdict + `warn=near-miss:<name>` note; (b) a new-signal warn when a board-signal NAME absent from the probe-time snapshot is now present — the name set baked as a constant derived from the committed fixtures' key set; both surface as verdict-line `warn=` notes (B29-style — the `.sh` wrapper's `head -n 1` drops any non-verdict stdout line, so a standalone stdout warn is never visible). **B27 parity pin: a B-test asserts the baked new-signal name set == the union of the committed fixtures' `.names` inventories ∩ board-signal families — a new fixture name trips CI until the constant is regenerated (the same names-parity class B23's P2 pin enforces for the predicate).**
**Step 3:** C1: wrap the unconditional `list_cards` block in `if not ctx.is_no_board:` (guard + dup-search stay git-local; symbol-without-slug → fail-closed UNKNOWN by design); C2/C5 skip branches; C3 refine (replace partial; retain `missing-card-context` guard); C4 refine (no-board branch fetch→behind→symbol-gated `_pickaxe_result`→skip; delete vacuous CLEAR; retain guard).
**Step 4:** `GitOps.remote_url()` = `git -C <resolved> remote get-url origin` (trimmed; fail → `"unknown"`); cwd-based ops-target resolution (`--repo` → `PARALLEL_CHECK_REPO` → cwd → `_REPO_ROOT`, with empty/whitespace env values falling through — never presence-keyed); token-repo decoupled (remote of resolved target or `"unknown"`, never `_REPO_ROOT`'s); `token_repo` + `mode` into `_apply_token`; **`_apply_token` writes atomically** — serialize to a temp file in `dirname(token_file)` (same-directory rename) with a per-invocation-unique tmp NAME (pid/random — a fixed name lets two concurrent writers interleave on ONE tmp path) + `os.rename` (mirrors the enforcer's own atomic bridge-write at index.ts:118-119; closes the corrupt-write race at the source). **`_apply_token` RETURNS an optional write-failure note that `run_check` appends to the verdict line as `warn=token-write-failed` (the B29 operator-visible surface — the current `except OSError: pass` swallows the failure and must be threaded through).** **`_apply_token` cleans its tmp file in a `finally` block (exception paths included) and, on write failure, UNLINKS any existing token at the target path — restoring the enforcer's "none found" BLOCK backstop (see B29 stale-token extension; the `finally` cannot run under the watchdog's SIGKILL, so the `.sh` error branch glob-cleans `*.tmp.*` too — see Task 1 Step 3).** **Also update `main()`'s defense-in-depth exception path (~823) — SIGNATURE SYNC under the new `_apply_token` contract** (it gains `mode`/`repo` parameters; the path constructs an UNKNOWN verdict and `_apply_token` already writes ONLY on CLEAR / unlinks on UNKNOWN — so no CLEAR-shaped token is at risk; the requirement is compatibility, not a phantom hazard). Keep the belt-and-suspenders guard: when repo/mode are unknowable, never write a CLEAR-shaped token. **`run_check`'s budget parse gains the SAME clamp (named implementation for the B23 non-finite/upper-bound fold): `budget = min(float(raw), PARALLEL_CHECK_BUDGET_MAX)` after the parse, with the same env-read constant + default 60 + `except (ValueError, OverflowError)` → default — today `float("inf")`/`"nan"`/`"1e309"` parse WITHOUT ValueError and `deadline = monotonic() + inf` never fires (without this, the non-finite fold's "no token written" assertion would go red with a CLEAR token); add the budget block to the Files-modify list.**
**Step 5:** Update the module docstring INTERFACE block (advisory + mode/repo fields + the `warn=` note contract + the five-family shared signal constants).
**Step 6:** Run: `python3.12 -m pytest scripts/test_parallel_work_check.py -x --timeout=60 -q` → green (32 existing incl. the timeout-contract sibling + B1–B34; xfail-or-xpass + skipif-supabase-v2 counted per environment). Regenerate `scripts/.vendor-patches/` + `scripts/.vendor-manifest.json` from FINAL files (ALL FOUR connector files incl. `__init__.py` + the patched `parallel_work_check.sh` + **`scripts/parallel_work_check.py` — its patch MUST be regenerated from the FINAL file (probe + all the new no-board machinery), not the pre-task snapshot, so the reverse-apply drift gate stays green** — now patched files with `identical@base` retained as the patch source) **and UPDATE the VENDOR.md prose ledger entry for the .sh patch to the FINAL semantics — unlink on `rc != 0` OR non-CLEAR first-line verdict + scoped tmp-glob + `PARALLEL_CHECK_BUDGET_MAX` clamp + setsid/group-kill — the durable ledger must describe the shipped patch, not the Task 1 snapshot**; `scripts/check-vendor-drift.sh --manifest` → exit 0. Commit.

### Task 3: Enforcer repo binding + no-board-skip audit (Phase C, TDD)

**Intent:** Gate-side contract safety — the real-token path honors `token.repo` (closing the #378/#18393 cross-tenant hole), skip tokens advance with a distinct audit, and no silent surface exists (State-1/State-2 guidance).

**Acceptance:** Mode-aware `checkpointTokenOk(ownerStep, {enforceBinding})` (binding only in gate/strict) consumed at all four call sites (805/926/1353/1545); the skip audit is emitted AT the two token-driven advance branches (real-token ok ~1395 + tool_result-marker ~1551), keyed on the mode-aware token check's result — NEVER inside `checkpointTokenOk` itself (it runs at all four call sites; see Step 2) — the real-token ok advance (~1395) AND the tool_result-marker advance (~1551, which is gated on token ok: `if (!marker.ok && st.ok) { consumeForceFile(...); advanceCheckpoint(...) }` — the canonical gate-mode no-board flow: pending checkpoint → checker run as the escape call → token produced → marker advance); the warn-mode first-call advance (~1375) is NOT token-driven (fires regardless of token state; non-gating — it emits `checkpoint_skipped_warn` only, and the no-board distinction is intentionally not needed there); binding-fail → named-reason BLOCK + State-1 + allowed re-run (never silent-stall/pass); `checkpoint_no_board_skip` audit at both token-driven advances + **negative audit tests (a normal CLEAR advance emits NO skip audit; a wrong-repo skip token BLOCKs with NO audit; `checkpoint_force_pass` is emitted ONLY when the operator's force file actually drove the pass — see the `consumeForceFile` behavior change in Step 2; future-`ts` real-token BLOCK — see T16)**; `bindingRepo() = resolveRepo(PARALLEL_CHECK_REPO) || currentRepo()` — `resolveRepo(v)` runs the SAME `git -C <v> remote get-url origin` (trimmed, fail → `"unknown"`) the checker's `GitOps.remote_url()` runs, because `PARALLEL_CHECK_REPO` is documented as a PATH while `token.repo` carries the URL form (comparing the raw env string would BLOCK every documented usage); 129 existing + T1–T16 green (incl. legacy-fixture policy + escape-matrix positives + frozen old-contract proxy + truncate/partial-write fixture + joint checker→enforcer test + shared-token semantics + unset-AGENT_INFRA_PATH fail-open guard + future-ts rejection); audit hygiene (`wc -l ~/.pi/agent/audit/enforcement.jsonl` unchanged).

**Files:**
- Modify: `extensions/sequence-enforcer/index.ts` (mode-aware wrapper, binding, audit event, auditLog failure surface, State-1/State-2 guidance, gateGuidance L819 path + L814 comment)
- Modify: `extensions/sequence-enforcer/sequence-enforcer.test.ts` (T1–T16 + fixture policy + escape-matrix positives)

**Step 1 (tests first):** `writeToken` gains default `repo: TEST_REPO` + `_setRepoForTest(TEST_REPO)` in affected sections (per-test `finally { _setRepoForTest(null) }` reset idiom) + one repo-less BLOCK variant; T1–T16: T1–T8 core (match/mismatch/both-unknown/missing-field; mode-tolerance; skip-mode advance audit at BOTH token-driven advance sites (real-token ~1395 + tool_result-marker ~1551); force-path unchanged; cross-cwd worktree pass / different-repo BLOCK / one-sided override BLOCKs ×2 / unknown-unknown vs unknown-vs-remote; guidance/escape UX at binding-fail — State-1 + allowed re-run; **path-valued `PARALLEL_CHECK_REPO` parity test — env set to a PATH + matching `token.repo` → pass; divergent path → named-reason BLOCK (pins the `resolveRepo` contract)**); **T6 negative-direction extensions:** (a) advance with a `mode:""` CLEAR token → `checkpoint_no_board_skip` NOT present in the captured audit sink; (b) advance with a skip-mode token whose repo mismatches → BLOCK + no skip audit; (c) assert `checkpoint_force_pass` is NOT co-emitted when a present force file is consumed on a real-token-wins advance — valid AFTER the `consumeForceFile` behavior change in Step 2; (d) assert the skip-audit event carries distinct mode-domain fields (`token_mode` vs enforcer `mode` — mutual-exclusivity enforced by data shape, not prose) AND the repo/phase fields the Task 4 Step 2 tripwire queries reference, so the tripwire's data contract is CI-pinned, not prose-pinned; (e) the skip audit fires on the tool_result-marker advance too; (f) exactly ONE skip audit per advance — no emissions on non-advancing token checks (blocked calls, guidance renders); (g) a marker advance with a present force file → consumed, no `checkpoint_force_pass` audit (the ~1550 site gets the same viaForce criterion); (h) a VIAFORCE-driven advance (real token FAILS, force file passes) with a stale no-board-skip-mode token present → `checkpoint_force_pass` audited, `checkpoint_no_board_skip` NOT present (the skip-audit emission is keyed on `ok && mode === "no-board-skip"` with a `!viaForce` guard — without the guard a force-driven advance would emit a spurious skip audit); (i) audit-sink WRITE failure (monkeypatch the append to raise) at EACH token-driven advance site → no throw escapes to the tool_call handler's fail-open catch (~1495-1516), the advance outcome is unchanged, and the failure is surfaced in the tool result or logged deterministically — never silent (the post-merge lag tripwire cannot distinguish "event never emitted" from "sink failed", so the failure must not be silent); **T8 variant (same-repo URL-format divergence):** two `git init` clones of one bare origin with deliberately divergent origin URL forms (`https://…/r.git` vs `git@…:r.git` or a trailing-slash variant) → assert the named-reason BLOCK, pinning that same-repo-different-format also fails (doc must match — see Task 4 Step 2); **T9 (State-1 guidance shape):** a start-phase step at binding-fail → gateGuidance emits the `env CHECKOUT_GUARD_ENFORCE=1` prefix + the resolved absolute path form; **T10 (frozen old-contract proxy + partial-write fixtures):** (a) read a no-board-skip token fixture (`mode`+`repo`+unknown fields present) with an explicit minimal reader (only `phase`/`verdict`/`ts`) → asserts advance — the regression net for the additive contract promise (T5 self-tests the new code; T10 pins that a minimal old-contract reader survives future field additions + checker drift); (b) a truncate/partial-write token fixture → named corrupt-token reason + no advance; (c) a garbage/truncated FORCE-file fixture → named handling + no throw + no `checkpoint_force_pass` + the gate does NOT ALLOW (a parse throw here lands in the tool_call handler's fail-open catch → silent ALLOW — the T15 hazard class, pinned for the force file); (d) a chmod-000 (unreadable) token file → named corrupt/unreadable reason, NOT a throw reaching the fail-open catch; (e) valid-JSON-but-wrong-SHAPE token fixtures (`null`, `[]`, scalar, `{}`) → a NAMED corrupt-token reason (the current code's `if (token)` falsy path leaves `reason` empty → an empty-reason BLOCK — a diagnosability hole in the exact class T10(b) claims to pin), no throw reaching the fail-open catch, no advance. **T11 (JOINT cross-language test — the two-language seam as one executable assertion, closing the field-name-typo drift class):** exec the REAL vendored checker (`.sh`/`run_check` with a no-board env + temp token file) to produce an ACTUAL token → feed it to the real `checkpointTokenOk` (NODE_ENV=test + `_setTokenFileForTest` + `_setRepoForTest`) → assert ok + the no-board-skip audit path; a repo-mismatch variant → named-reason BLOCK; ALSO feed the checker's actual output through the T10 minimal old-contract reader → assert advance (the WRITE side of the deploy-window contract, so token-shape drift goes CI-red independent of manifest regeneration). **T12 (shared-token semantics = contract, not accident):** after an ok-advance the token file still EXISTS (checkpointTokenOk never unlinks it — only the force file is one-shot); a second same-phase/same-repo checkpoint advances on the same file. **T13 (interleaved plan→implement):** write a plan-phase token, overwrite with an implement-phase token → the plan gate BLOCKs (phase), the implement gate passes on the other session's token (pins last-writer-wins phase semantics). **T14 (repo env/flag edge):** empty `PARALLEL_CHECK_REPO` → `currentRepo()`, never a bound `""`; `--repo X` from cwd Y (no env set) → named-mismatch BLOCK + guidance pointing at the `PARALLEL_CHECK_REPO` remediation; **`resolveRepo` bad-path contract (CI-pinned, not prose): `PARALLEL_CHECK_REPO` = nonexistent path, plain file, and non-git dir → returns `"unknown"`, binding passes via both-"unknown" — a throw here would land in the binding surface's documented fail-open catch (~1496) and convert BLOCK→ALLOW (the T15 hazard class, now pinned for the new function).** **T15 (unset-AGENT_INFRA_PATH guidance — fail-open guard):** with AGENT_INFRA_PATH UNSET at a blocked call, gateGuidance must NOT throw — the tool_call handler's catch is documented "fail-open is DELIBERATE" (~1495-1516), so a TypeError would convert the pending-checkpoint BLOCK into an ALLOW, silently ungating exactly the no-board consumer population this plan targets — assert no throw + the "set AGENT_INFRA_PATH" instruction text (L814 intent); **plus the SET-but-NONEXISTENT variant: no throw, guidance prints the runnable form with an explicit "path does not exist" hint distinguishing it from the unset state.** **T16 (future-ts real-token):** a CLEAR token with `ts` in the future (now + 30 min) → named-reason BLOCK — symmetry with the force-file path's existing future-ts rejection (index.ts:767); without it, a clock-skewed/edited token never TTL-expires and, in no-board mode where all phases CLEAR, ONE tampered token satisfies every gate in a session. **This REVERSES a deliberately pinned behavior at sequence-enforcer.test.ts:273-278 ("future-ts skew (now+300s) → ok until the skew passes (pinned)" — negative elapsed = inside TTL); that test is flipped in Step 2 with the tamper rationale.** escape-matrix positives `env CHECKOUT_GUARD_ENFORCE=1 <abs>/scripts/parallel_work_check.sh start` + `env AGENT_INFRA_PATH=<abs> <abs>/scripts/parallel_work_check.sh plan`. Run → red.
**Step 2:** Widen the inline token type + return (`{ok, reason, viaForce?, mode?}`); implement the mode-aware wrapper (warn → binding-free; gate/strict → `token.repo === bindingRepo()`, with `bindingRepo() = resolveRepo(PARALLEL_CHECK_REPO) || currentRepo()` — the SAME `git -C <v> remote get-url origin` resolution the checker uses, so path-valued env works); **extend the real-token staleness branch in `checkpointTokenOk` to reject `ts > Date.now()` (future ts) with a named reason, mirroring the force-file rejection at index.ts:767 (pinned by T16), and FLIP the existing pinned test at sequence-enforcer.test.ts:273-278 (`future-ts skew (now+300s) → ok until the skew passes` — asserts negative elapsed = inside TTL) to assert the named-reason BLOCK — T16 reverses a deliberately pinned behavior with the tamper rationale (one tampered future-ts token would otherwise satisfy every gate in a no-board session); the flip list for Task 3 is then THREE existing tests: real-token-wins ~1526-1553, warn-mode-consume ~966-986, future-ts-skew 273-278**; wire binding + `checkpoint_no_board_skip` audit AT the two token-driven advance branches — real-token ok (~1395) + tool_result-marker (~1551, gated on token ok: the canonical no-board flow — pending checkpoint → checker run as the escape call → token produced → marker advance) — NOT inside `checkpointTokenOk` itself (it runs at ALL FOUR call sites incl. gateGuidance's internal state check ~805 and validateToolCall ~926, which would emit the audit 2-4× per advance and on non-advancing calls; emission count pinned by T6(f)); the warn-mode first-call advance (~1375) is NOT token-driven (fires regardless of token state; non-gating — `checkpoint_skipped_warn` only; the no-board distinction is intentionally not needed there); thread the named binding reason at the validateToolCall pending-escape branch (~992). **`consumeForceFile` (~619) behavior change (T6(c) requires it) — ONE pinned criterion: emit `checkpoint_force_pass` ONLY when the advance was actually force-driven (`tokenState.viaForce`) — the operator's force file truly drove the pass (a warn-mode consume, or a consume alongside a real-token advance, emits nothing; ALL THREE `consumeForceFile` call sites — 1374 warn / 1394 real-token / 1550 marker — pass the token-state context; **the criterion is MODE-QUALIFIED: `tokenState.viaForce && mode !== "warn"` — `checkpointTokenOk` returns `{ok:true, viaForce:true}` whenever a passable force file exists with no ok real token (the exact warn-mode-consume scenario the flipped ~966-986 test pins as "consumed, no force_pass audit"), so viaForce alone would emit in warn mode; the warn site passes a force-suppressed context**; the marker site is pinned by T6(g)); still UNLINK on any advance (the one-shot invariant is preserved). Update BOTH existing tests: real-token-wins (~1526–1553) AND warn-mode-consume (~966–986, which currently asserts `checkpoint_force_pass` IS audited at 981-982) → each flipped to "consumed, no force_pass audit".** **`auditLog` (~346-358) silent-catch change (named implementation for T6(i)'s "never silent" assertion): the `catch { /* fail silently — audit is best-effort */ }` becomes a deterministic surface — `console.warn` on failure OR a returned failure flag consumed at the advance sites — so the sink-failure is surfaced, not silent (today's code is already non-throwing, so T6(i)'s "no throw escapes" half passes on current behavior; the "never silent" half requires this change; add `auditLog` failure surface to the Files-modify list).**
**Step 3:** State-1: ENFORCE-prefix emission for `requiredPhase === "start"`; update the L814 comment ("default `$AGENT_INFRA_PATH` scripts dir — a required prerequisite per AGENTS.md; guidance says 'set AGENT_INFRA_PATH' when unset — there is NO `$HOME/agent-infra` fallback fiction: agent-infra lives at `$HOME/Documents/GitHub/agent-infra` here, and pointing at a nonexistent default path is worse than saying so"); L819 bin → **THROW-SAFE resolution** — build `join(AGENT_INFRA_PATH, "scripts", "parallel_work_check.sh")` ONLY when `AGENT_INFRA_PATH` is set (`PARALLEL_CHECK_BIN` first); when both are absent, guidance emits the 'set AGENT_INFRA_PATH' instruction (L814 intent). **Why throw-safe: gateGuidance runs in the blocked-call branch inside the tool_call handler's try/catch whose catch is documented "fail-open is DELIBERATE" (~1495-1516) — a TypeError would convert the pending-checkpoint BLOCK into an ALLOW, silently ungating exactly the no-board consumer population this plan targets (pinned by T15).** The guidance PRINTS the resolved absolute path WITHOUT an `env PARALLEL_CHECK_BIN=…` prefix (the escape-regex allowlist is `GH_TOKEN|CHECKOUT_GUARD_ENFORCE|AGENT_INFRA_PATH` — `PARALLEL_CHECK_BIN` is NOT allowed, so an env-prefixed print would be unexecutable at the gate; `PARALLEL_CHECK_BIN` remains only an internal bin-location override, consistent with Task 4 Step 1's "do NOT run `$PARALLEL_CHECK_BIN` at the gate"). State-2: append the no-board-skip note when `mode === "no-board-skip"`.
**Step 4:** Run: `npx tsx extensions/sequence-enforcer/sequence-enforcer.test.ts` → 129 + new green (~2.2s); `wc -l ~/.pi/agent/audit/enforcement.jsonl` unchanged. Commit.

### Task 4: Skill/path rewrites + enforcement SKILL.md (Phase D)

**Intent:** Every hardcoded swarm path points at the canonical `$AGENT_INFRA_PATH/scripts` location with escape-safe run forms; the enforcement SKILL.md fiction (target 3) is written; no-board semantics are visible in skill text.

**Acceptance:** `grep -rn "/Users/danielospina/swarm/operations/coordination"` matches ONLY `docs/` (the historical #357 scoping doc + this PR's #383 docs, which legitimately record the old path as provenance — do NOT rewrite history) and ZERO refs in `skills/`, `extensions/`, `scripts/`. The not-touched list is context, not grep targets: enforcer test:516 uses a synthetic `/repo/operations/…` fixture, the vendored test file's swarm strings are the `daniel-ospina/swarm` slug only, and the 4 slack-bridge `approvals.json` refs use a `<dir>/swarm/…` relative form — none match the grep string, so an implementer must not chase them. Enforcement SKILL.md contains `$AGENT_INFRA_PATH/scripts` (grep ≥ 1) + the no-board bullet + updated trust boundary + audit-set line; 6 skill comment instances carry the escape-safe canonical wording (incl. `env CHECKOUT_GUARD_ENFORCE=1` at C1, "pending-gate guidance prints the resolved command", suffix clause).

**Files:**
- Modify: `extensions/sequence-enforcer/index.ts` (gateGuidance L819 — done in Task 3 step 3)
- Modify: `skills/writing-plans/SKILL.md:31`, `skills/commit-workflow/SKILL.md:17`, `skills/executing-plans/SKILL.md:15+42`, `skills/issue-scoping/SKILL.md:87-95`, `skills/enforcement/SKILL.md` (L28 insert + L29→30 escape + L31→32 trust boundary + L35→36 audit set)

**Step 1:** Rewrite the 6 hardcoded path instances to the canonical escape-safe wording (S11 pin): "resolve `$AGENT_INFRA_PATH` — a required prerequisite per AGENTS.md (NO `$HOME/agent-infra` default fiction; if unset, the pending-gate guidance says to set it) — and run the absolute path `…/scripts/parallel_work_check.sh <phase>`; at C1/start also run `env CHECKOUT_GUARD_ENFORCE=1 <resolved-path> <phase>`; the pending-gate guidance prints the resolved command — use that form (it is escape-regex-safe); do NOT run `$PARALLEL_CHECK_BIN`/`env PARALLEL_CHECK_BIN=…` at the gate (not in the escape allowlist); `.sh|.py` suffix required".
**Step 2:** Enforcement SKILL.md: insert the No-board skip (gate/strict) bullet after the warn-mode bullet; write the `$AGENT_INFRA_PATH/scripts` canonical reference in the escape section; replace the trust-boundary text ("no repo binding — #378/#18393") with the pinned repo-bound contract (URL form, both-"unknown" for no-remote/non-git-cwd, cross-repo BLOCK → re-run, `PARALLEL_CHECK_REPO` honored as a PATH resolved via the same `git remote get-url` as the checker — set-but-not-a-git-dir → both-"unknown", `--repo X` without env binds X's remote — and `--repo X` from a different cwd with no env means the enforcer never learns the flag → named-reason BLOCK with the remediation "set PARALLEL_CHECK_REPO to the checkout path", **same-repo-different-URL-format BLOCKs too — re-run from the same checkout form**; the wording must not promise "a token written in repo A fails repo B" implies same-repo always passes); align the operator-force-path wording with the `consumeForceFile` change (the force file is consumed on any advance; it is AUDITED as `checkpoint_force_pass` only when it actually drove the pass); audit-set line += `checkpoint_no_board_skip` + mutual-exclusivity + mode-domain clause; audit-gap note for the deploy window; **same-repo concurrent-session note:** the token file is last-writer-wins, machine-global, NEVER unlinked on a token-driven advance (only the force file is one-shot); phase+TTL+repo-binding bound the class; a shared-token advance may be audit-attributed to either concurrent session — accepted, documented. **Mode-attribution sub-case (pinned by T12/T13, documented here):** a no-board session advancing on a concurrent board session's CLEAR token emits a BOARD-mode audit with no skip event — the post-merge lag tripwire cannot see that (the event stream carries no tenant info); add the tripwire query for same-repo/same-phase multi-advance anomalies alongside the existing no-board-lacks-event query (per-session nonce deferred — see the deferral record below).
**Step 3:** Issue-scoping prose block: add the no-board sentence. Run the acceptance greps; `bash -n` unaffected. Commit.

### Task 5: CI wiring (Phase E)

**Intent:** The vendored suite + enforcer suite run in CI (per-PR + post-merge), the reusable python-ci.yml is finally called, and drift is caught — without the base-ref landing trap.

**Acceptance:** ci.yml `verify` job green (pytest + tsx); ci.yml `vendor-drift` job green (bash -n + `--manifest` — the single drift gate); ci-main.yml `python-ci` job green (post-merge, `install-extras: ''`); python-ci.yml skip-empty guard materialized (template == committed); pipeline-compliance `workflow-drift` green.

**Files:**
- Modify: `templates/.github/workflows/python-ci.yml` + re-materialize `.github/workflows/python-ci.yml` via `scripts/sync-ci-workflows.sh`
- Modify: `.github/workflows/ci.yml` (add `verify` + `vendor-drift` jobs)
- Modify: `.github/workflows/ci-main.yml` (add `python-ci` job)

**Step 1:** Guard the python-ci.yml install step in BOTH jobs: `if: inputs.install-extras != '' && (hashFiles('pyproject.toml') != '' || hashFiles('setup.py') != '' || hashFiles('setup.cfg') != '')` (template default stays `'.'`). Run `scripts/sync-ci-workflows.sh`; verify `.github/workflows/python-ci.yml` == template.
**Step 2:** ci.yml: add the self-contained `verify` job (checkout → setup-node 22 → setup-python 3.12 → `pip install pytest pytest-timeout` → `python3.12 -m pytest scripts/test_parallel_work_check.py -x --timeout=60 -q` → `npx tsx extensions/sequence-enforcer/sequence-enforcer.test.ts` → `! grep -rE '/Users/danielospina/swarm/operations/coordination' skills/ extensions/ scripts/` (fail on match — the Task 4 path-fence acceptance becomes CI-enforced, machine-checked not manual; **`VENDOR.md` + `scripts/check-vendor-drift.sh` MUST use `$SWARM_ROOT`/`$HOME`-derived paths and NEVER the literal `/Users/danielospina/swarm/operations/coordination` — the live mode is already `$SWARM_ROOT` env-driven per Task 1 Step 5, so no exception clause exists and both the Task 4 acceptance and this grep pass without one**) — NO drift tail here (T11's joint checker→enforcer test lives in the tsx suite, so token-shape drift goes CI-red via the verify job INDEPENDENT of manifest regeneration). **ONE canonical interpreter (3.12) across Task 1 local, this per-PR verify job, and the ci-main python-ci caller — a 3.11/3.12 divergence would let a 3.11-only failure pass PR CI and only break post-merge, triggering the false "main broken by merge" auto-file the plan guards against.** + the `vendor-drift` job (`bash -n scripts/checkout_guard.sh scripts/parallel_work_check.sh scripts/check-vendor-drift.sh` → `scripts/check-vendor-drift.sh --manifest`) as the SINGLE drift gate (isolation rationale: drift failures get their own red signal, decoupled from the pytest/tsx suite, so a vendor sync lands as one identifiable check — no duplicate execution).
**Step 3:** ci-main.yml: add the `python-ci` job (`uses: daniel-ospina/agent-infra/.github/workflows/python-ci.yml@main` + `secrets: inherit` + `with: { python-version: '3.12', test-command: 'python3.12 -m pytest scripts/test_parallel_work_check.py -x --timeout=60 -q', lint-command: '', typecheck-command: '', install-extras: '' }` — **python-version MUST be '3.12' to match the test-command: setup-python installs pytest/pytest-timeout into the 3.12 toolcache env, and the runner's system `python3.12` has neither (a '3.11' + 'python3.12' mismatch would fail every post-merge run and, via the new auto-file-on-failure entry, auto-file false "main broken by merge" issues)**) **AND add `python-ci` to the `auto-file-on-failure` job's `needs:` list (currently `[extension-tests, script-validate, skill-lint, cost-config-guard]`) so a post-merge python-ci failure auto-files the "main broken by merge" issue**. Local pre-flight: `bash -n scripts/*.sh`; `scripts/check-vendor-drift.sh --manifest` → exit 0. Commit.

### Task 6: Realistic probe + O/I/T verification (Phase F)

**Intent:** Prove the Objective end-to-end on real no-board consumer environments, and set up the post-merge deploy verification — the hard acceptance gates.

**Acceptance:** B17/B21 green (probe + consumer-env fixtures from Task 1 Step 6); Phase F step 27 pre-merge probe passes — **exercising the PRE-FIX deployed gate (deploy-window semantics):** fresh tortoise/DMeer session env (captured per Task 1 Step 6) → skip fires + the OLD deployed enforcer tolerates the new token (parse-safety) + gate advances; real-session assertion: PI_*/GH_TOKEN/PATH present + capture-context repo affinity; O/I/T battery below passes; contingency fork executed if a collision is found (human-approval gate — see Task 6 Step 2).

**Files:**
- Modify: `scripts/test_parallel_work_check.py` (B17/B21 — done in Task 2; verify here)
- Execute: manual probes + verification battery

**Step 1:** Run B17 (fresh consumer checkout: `git init -b main` + commit + local bare origin; minimal env, no board signals → C1/C3/C4 `CLEAR no-board-skip`; re-run with ONLY `AGENT_ID` → fail-closed UNKNOWN) + B21 (consumer-env fixture assertion — incl. the ≥ 2 distinct-origin minimum-fixture gate) — green.
**Step 2:** O/I/T battery: (a) `git ls-files` shows the vendored set; (b) no-board session passes plan+implement checkpoints without force-pass — **pinned mechanics, no fakeable evidence: launch a fresh `pi -p` INSIDE the consumer checkout with the captured env (Task 1 Step 6) → activate the executing-plans `start` checkpoint → run the vendored checker → observe the advance via the DEPLOYED extension's audit (`~/.pi/agent/audit/enforcement.jsonl`) + console output; PROVE the deployed copy was loaded, not the branch source: verify the loaded extension md5 matches `~/.pi/agent/extensions/sequence-enforcer` (NOT the branch checkout). Honest scope: the pre-merge probe proves skip firing + predicate efficacy + OLD-enforcer tolerance (deploy-window semantics); NEW-gate repo-binding acceptance in a real session is proven by the MANDATORY in-suite joint test T11 plus this manual re-run through the NODE_ENV=test harness against the new `index.ts` (not optional), and definitively by the post-merge probe (Step 3)**; (c) swarm behavior unchanged — 34-parity tests + board-mode paths byte-identical (ledger P1–Pn only) **+ a swarm-style board session's C1 verdicts unchanged (B33(a) + probe assertion: board session with `CHECKOUT_GUARD_SWARM_ROOT` set → C1 guard/dup verdicts match the pre-vendor invocation; the unset default diverges — documented in VENDOR.md)**; (d) enforcement SKILL.md `$AGENT_INFRA_PATH/scripts` reference matches reality — **assert AGENT_INFRA_PATH is SET in the probe session and resolves to the live `scripts/` dir (not just grep ≥ 1)**. Consumer-env signature verified PRE-MERGE (Task 1 Step 6 capture + B21) — the contingency fork (narrowing the predicate or adding the per-session nonce) requires explicit human approval BEFORE merge, presented with the capture results; a vacuous capture (implementer-session env, no consumer repo affinity) fails the gate.
**Step 3 (post-merge, documented — not in the PR diff):** #357 deploy-sync + md5 gate; verify the DEPLOYED enforcer emits `checkpoint_no_board_skip` in a live no-board session; lag tripwire (md5-vs-source auto-file or audit query for no-board passes lacking the event); enforcement SKILL.md + 6 skill refs propagate via link-skills.sh. **Deploy-window reverse edge (pinned):** tokens written PRE-deploy carry the abspath-form `repo` (the current `_apply_token` writes `repo_path`), so after the new enforcer deploys they BLOCK with a named repo-mismatch until the checker re-runs (T8 pins the class; the remedy is "re-run the checker once"); the lag tripwire also queries for a burst of repo-mismatch blocks in the deploy window. Commit.

---

**Execution mode:** Subagent-Driven (6 tasks ≤ 8) — this session, fresh subagent per task + code review. **Sequencing (corrected — the earlier T2∥T3 parallel claim was wrong):** Task 2 (checker predicate) runs FIRST, then Task 3 (enforcer binding). T11 (Task 3's joint test) execs the REAL vendored checker and asserts the new mode/repo token contract — the pre-Task-2 checker writes no `mode` field and a path-form `repo`, so T11 stays red until Task 2's checker commit lands; the joint test forces the order. Task 3's non-joint tests (T1–T10, T12–T15) touch only TS files and CAN be drafted in parallel with Task 2, but the suite runs green only after T2 merges. Task 4 depends on Task 3's L819 edit; Task 6 depends on both. Attach the S4/S5 contract text to each sub-agent prompt.

**Deferred (GOOD > EASY record):** per-session token nonce/owner field — Good alternative for deterministic audit attribution of same-repo concurrent-session advances (reviewer-flagged). Cost: token schema change + T-series churn + checker/enforcer contract migration. Rationale: phase+TTL+repo-binding already bound the concurrency class to fail-safe outcomes; ambiguity is limited to audit attribution of a shared-token advance, documented as accepted behavior in enforcement SKILL.md (Task 4 Step 2). Revisit if audit forensics ever require per-session attribution.

---

<!-- plan-review: cycles=11, status=clean, version=2.3.0 -->

### Plan Review Cycle Log (#383)

| Cycle | Reviewers | P0 | P1 | P2/P3 | Fixed |
|---|---|---|---|---|---|
| 1 | Structural, Integration, Failure-Mode | 0 | 2 | 13 | capture-step owner, predicate value-semantics, connectors `__init__`, grep acceptance, T2∥T3, CI split, probe honesty, concurrency doc+deferral, atomic write, timeout sibling, negative audits, URL-format variant, old-contract proxy, write-failure UX, T9, intent rephrase, symbol-no-slug |
| 2 | Structural, Integration, Failure-Mode | 0 | 2 | 9 | `_sb_config`-exact signal set, `resolveRepo` path-parity, consumeForceFile viaForce change, advance-site labels, python3.12 pin, connector provenance, redact-then-diff, `$HOME/agent-infra` fiction, PARALLEL_CHECK_BIN print rule, probe md5 mechanics, skip-count qualification |
| 3 | Combined + Failure-Mode | 0 | 1 | 8 | tool_result-marker audit (canonical no-board flow), T11-forced T2→T3 sequencing, 8-name set, B33 two-case guard parity, throw-safe L819 (fail-open guard), B21 vacuity, origin/main assumption, B29 stream/order, main() signature-sync |
| 4 | Combined + Failure-Mode | 0 | 0 | 9 | interpreter pins everywhere, map counts, B31 phase-split, B23 rationale, ONE viaForce criterion + both tests, B29 verdict-note, auto-file needs, audit emission anchor, deploy-window reverse edge |
| 5 | Combined + Failure-Mode | 0 | 1 | 8 | python-ci 3.11/3.12 mismatch, three consumeForceFile sites, B27 mechanism, Files B1–B33, C1:UNKNOWN assertion, write-failure propagation, tmp-name unique, five-family parity, future-ts T16, min-fixture gate, GH_API_BASE fold |
| 6 | Combined + Failure-Mode | 0 | 2 | 7 | watchdog unlink .sh patch, timeout edge-values, force-file/unreadable-token throw fixtures, tmp residue, AGENT_INFRA_PATH nonexistent variant, B27 parity, event schema fields, count inversion |
| 7 | Combined + Failure-Mode | 0 | 1 | 7 | unlink-failure silent corner (warn=unlink-failed + non-CLEAR-verdict unlink), scoped tmp-glob + naming contract, budget clamp VALUE (env default 60 + injectable), non-finite timeout values, B34 deterministic barrier variant, T10 wrong-shape JSON, heartbeat contract |
| 8 | Combined + Failure-Mode | 0 | 0 | 3 | python-side budget clamp named, auditLog silent-catch change named, VENDOR.md/check-vendor-drift.sh literal-free via `$SWARM_ROOT` |
| 9 | Exit verification | 0 | 0 | 0 | — |

Converged clean after 9 cycles (10 dispatches incl. the cap-tracked combined passes); all fixes evidence-backed against the working tree, swarm @ `29dd67e8`, and the enforcer source.
